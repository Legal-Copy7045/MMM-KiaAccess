# GENERATED by scripts/sync-core.js from core/commands.json — do not edit.

lock:
  name: "Lock"
  description: "Lock — Kia Access"
  fields:
    entry_id:
      name: Account
      description: Config entry id (only needed with multiple accounts).
      required: false
      selector:
        config_entry:
          integration: kia_access

unlock:
  name: "Unlock"
  description: "Unlock — Kia Access"
  fields:
    entry_id:
      name: Account
      description: Config entry id (only needed with multiple accounts).
      required: false
      selector:
        config_entry:
          integration: kia_access

flash_lights:
  name: "Flash hazards"
  description: "Flash hazards — Kia Access"
  fields:
    entry_id:
      name: Account
      description: Config entry id (only needed with multiple accounts).
      required: false
      selector:
        config_entry:
          integration: kia_access

find_car:
  name: "Flash and honk"
  description: "Flash and honk — Kia Access"
  fields:
    entry_id:
      name: Account
      description: Config entry id (only needed with multiple accounts).
      required: false
      selector:
        config_entry:
          integration: kia_access

open_charge_port:
  name: "Open charge port"
  description: "Open charge port — Kia Access"
  fields:
    entry_id:
      name: Account
      description: Config entry id (only needed with multiple accounts).
      required: false
      selector:
        config_entry:
          integration: kia_access

close_charge_port:
  name: "Close charge port"
  description: "Close charge port — Kia Access"
  fields:
    entry_id:
      name: Account
      description: Config entry id (only needed with multiple accounts).
      required: false
      selector:
        config_entry:
          integration: kia_access

start_climate:
  name: "Start climate"
  description: "Start climate — Kia Access"
  fields:
    entry_id:
      name: Account
      description: Config entry id (only needed with multiple accounts).
      required: false
      selector:
        config_entry:
          integration: kia_access
    set_temp:
      name: "Set temp"
      required: false
      default: 70
      selector:
        number:
          min: 62
          max: 82
          step: 1
          mode: "box"
          unit_of_measurement: "°F"
    duration:
      name: "Duration"
      required: false
      default: 10
      selector:
        number:
          min: 1
          max: 30
          mode: "box"
          unit_of_measurement: "min"
    climate:
      name: "Climate"
      required: false
      default: true
      selector:
        boolean: {}
    defrost:
      name: "Defrost"
      required: false
      default: false
      selector:
        boolean: {}
    heating:
      name: "Heating"
      required: false
      default: 0
      selector:
        number:
          min: 0
          max: 4
          mode: "box"
    steering_wheel:
      name: "Steering wheel"
      required: false
      default: 0
      selector:
        number:
          min: 0
          max: 2
          mode: "box"
    front_left_seat:
      name: "Front left seat"
      required: false
      default: 0
      selector:
        number:
          min: 0
          max: 8
          mode: "box"
    front_right_seat:
      name: "Front right seat"
      required: false
      default: 0
      selector:
        number:
          min: 0
          max: 8
          mode: "box"
    rear_left_seat:
      name: "Rear left seat"
      required: false
      default: 0
      selector:
        number:
          min: 0
          max: 8
          mode: "box"
    rear_right_seat:
      name: "Rear right seat"
      required: false
      default: 0
      selector:
        number:
          min: 0
          max: 8
          mode: "box"

stop_climate:
  name: "Stop climate"
  description: "Stop climate — Kia Access"
  fields:
    entry_id:
      name: Account
      description: Config entry id (only needed with multiple accounts).
      required: false
      selector:
        config_entry:
          integration: kia_access

start_charge:
  name: "Start charging"
  description: "Start charging — Kia Access"
  fields:
    entry_id:
      name: Account
      description: Config entry id (only needed with multiple accounts).
      required: false
      selector:
        config_entry:
          integration: kia_access

stop_charge:
  name: "Stop charging"
  description: "Stop charging — Kia Access"
  fields:
    entry_id:
      name: Account
      description: Config entry id (only needed with multiple accounts).
      required: false
      selector:
        config_entry:
          integration: kia_access

set_charge_limits:
  name: "Set charge limits"
  description: "Set charge limits — Kia Access"
  fields:
    entry_id:
      name: Account
      description: Config entry id (only needed with multiple accounts).
      required: false
      selector:
        config_entry:
          integration: kia_access
    ac_limit:
      name: "Ac limit"
      required: false
      default: 80
      selector:
        number:
          min: 50
          max: 100
          mode: "box"
          unit_of_measurement: "%"
    dc_limit:
      name: "Dc limit"
      required: false
      default: 100
      selector:
        number:
          min: 50
          max: 100
          mode: "box"
          unit_of_measurement: "%"

send_to_car:
  name: "Send to car"
  description: "Send to car — Kia Access (not available in all regions yet — returns an error until it is)"
  fields:
    entry_id:
      name: Account
      description: Config entry id (only needed with multiple accounts).
      required: false
      selector:
        config_entry:
          integration: kia_access
    name:
      name: "Name"
      required: false
      default: "Destination"
      selector:
        text: {}
    address:
      name: "Address"
      required: false
      selector:
        text: {}
    latitude:
      name: "Latitude"
      required: false
      selector:
        number:
          min: -90
          max: 90
          step: 0.000001
          mode: "box"
    longitude:
      name: "Longitude"
      required: false
      selector:
        number:
          min: -180
          max: 180
          step: 0.000001
          mode: "box"
